{{header-nav back='back' forward='forward' invalidateSession='invalidateSession'}} {{outlet}} {{outlet "modal"}}

<div class="flash">
  {{#each flashMessages.queue as |flash|}}
    {{flash-message flash=flash}}
  {{/each}}
</div>
