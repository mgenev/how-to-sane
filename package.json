{
  "name": "how-to",
  "version": "0.0.1",
  "description": "A blank sane stack project.",
  "main": "index.js",
  "scripts": {
    "lint": "eslint client/app client/tests server/api server/tests server/config",
    "pretest": "npm run lint",
    "test": "(cd server && node tests/runner.js) && (cd client && node node_modules/.bin/ember test)"
  },
  "author": "",
  "license": "MIT",
  "dependencies": {},
  "devDependencies": {
    "babel-eslint": "^3.1.18",
    "eslint": "^0.23.0"
  }
}
